using MyDownloadManager.Core.Models;

namespace MyDownloadManager
{
    public static class Bootstrapper
    {
        public static void RegisterServices()
        {
            // TODO: register DI services (DownloadManagerService, repositories, etc.)
        }
    }
}
